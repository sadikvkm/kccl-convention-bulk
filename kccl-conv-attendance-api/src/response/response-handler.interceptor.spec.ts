import { ResponseHandlerInterceptor } from './response-handler.interceptor';

describe('ResponseHandlerInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseHandlerInterceptor()).toBeDefined();
  });
});
